extends layout

block content
  .container
      for post in wordpress.post
          h1= post.title
          //- p= post.date
          p= moment(post.date).format("MMMM DD, YYYY")
          .content!= post.content
          img(src=post.featured_image)
