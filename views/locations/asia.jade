extends ../layout

block content
    .container.index
        h1.pageHeading Asia
        .postWidget__wrapper
        for post in wordpress.post
            - for category in post.categories
                if  (category.name == "Asia")
                  include ../templates/post





