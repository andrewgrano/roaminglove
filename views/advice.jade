extends layout

block content
    .container.index
        h1.pageHeading Advice
        .postWidget__wrapper
        for post in wordpress.post
            - for tag in post.tags
                if  (tag.name == "Advice")
                  include templates/post
