extends layout

block content
    .container.index
        h1.pageHeading Photos
        .postWidget__wrapper
        for post in wordpress.post
            - for tag in post.tags
                if  (tag.name == "Photos")
                  include templates/post
