extends layout

block content
    .container.index
        h1.pageHeading Stories
        .postWidget__wrapper
        for post in wordpress.post
            - for tag in post.tags
                if  (tag.name == "Stories")
                  include templates/post
