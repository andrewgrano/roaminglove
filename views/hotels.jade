extends layout

block content
    .container.index
        h1.pageHeading Hotels
        .postWidget__wrapper
        for post in wordpress.post
            - for tag in post.tags
                if  (tag.name == "Hotels")
                  include templates/post





